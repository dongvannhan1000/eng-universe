{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "dev": { "cache": false, "persistent": true },
    "build": { "outputs": ["dist/**", "build/**"], "dependsOn": ["^build"] },
    "lint": {},
    "test": { "outputs": [".coverage/**"] }
  }
}
